import 'package:flutter_test/flutter_test.dart';

void main() {
  // Los tests de integración se migrarán cuando se configure un entorno
  // de testing con mocks de los servicios de la API.
  testWidgets('placeholder smoke test', (WidgetTester tester) async {
    expect(1 + 1, 2);
  });
}
